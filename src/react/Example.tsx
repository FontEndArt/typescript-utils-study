import React, { ReactNode } from 'react';

/**
 * ReactExample
 * @description A simple example of a React component
 * ```Typescript
 * import { ReactExample } from 'xxx'
 * const Example = () => {
 *     return <ReactExample />
 * }
 * export default Example
 * ```
 * @returns {ReactNode} example
 */
export const ReactExample = (): ReactNode => {
    return <div>example</div>
}
